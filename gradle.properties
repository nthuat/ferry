org.gradle.jvmargs=-Xmx3g -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official

# Publishing to Maven Central (gradle/publishing.gradle.kts). Never set real values here — this file
# is committed. Set them in an untracked ~/.gradle/gradle.properties instead, or via an environment
# variable of the same name in SCREAMING_SNAKE_CASE (e.g. MAVEN_CENTRAL_USERNAME). All four are
# optional: absent, the build still works, including ./gradlew :ferry:publishToMavenLocal.
#mavenCentralUsername=
#mavenCentralPassword=
#signingInMemoryKey=
#signingInMemoryKeyPassword=
